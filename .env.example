# Copy this file to `.env` for local development and replace values.
# Vercel: set these variables in Project → Settings → Environment Variables

# Backend URL (Render service URL). Do NOT include a trailing slash.
VITE_BACKEND_URL=https://your-render-app.onrender.com

# Supabase (optional) - public anon key only
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
 
# Frontend URL used as redirect target for email confirmations
# Set this to your Vercel URL (no trailing slash), e.g.:
# VITE_FRONTEND_URL=https://junior-debug-frontend-yourid.vercel.app

